import { Injectable } from '@nestjs/common';

@Injectable()
export class PostsService {
    // private readonly posts: PostsInfo[] = [];
    // getPosts(): Promise<PostsInfo[]> {
    //     return Promise.resolve(this.posts);
    // }
}
